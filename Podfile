# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftUI SQLite' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUI SQLite
    pod 'SQLite.swift', '~> 0.14.0'


  target 'SwiftUI SQLiteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftUI SQLiteUITests' do
    # Pods for testing
  end

end
